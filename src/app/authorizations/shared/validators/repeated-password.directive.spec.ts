import { RepeatedPasswordDirective } from './repeated-password.directive';

describe('RepeatedPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatedPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
